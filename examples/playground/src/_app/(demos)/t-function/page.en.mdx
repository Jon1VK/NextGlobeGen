import TranslationsExample from "./TranslationsExample";

# Translator Function

`t` function is returned by the `useTranslations` hook and the `createTranslator` function. It is used to translate the messages to the current locale in the given namespace.

See the [ICU docs](https://unicode-org.github.io/icu/userguide/format_parse/messages/) and the following examples for available interpolation patterns.

<TranslationsExample />
