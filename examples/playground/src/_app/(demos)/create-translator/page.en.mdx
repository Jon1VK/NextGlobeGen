import CreateTranslatorExample from "./CreateTranslatorExample";

# createTranslator

The `createTranslator` function returns a translator function for the given locale and namespace. This is useful in Server Actions and Route Handlers where the locale cannot be inferred from the rendering context.

<CreateTranslatorExample />
