import RevalidatePathExample from "./RevalidatePathExample";

# revalidatePath

The `revalidatePath` function extends Next.js `revalidatePath` to work with localized routes. This allows you to purge the cache for specific localized pages after data mutations.

<RevalidatePathExample />
