import UseRouterExample from "./UseRouterExample";

# useRouter

The `useRouter` hook extends the Next.js `useRouter` hook to allow programmatic navigations to localized pathnames.

<UseRouterExample />
