{
  "name": "monorepo",
  "version": "1.0.0",
  "description": "NextGlobeGen monorepo example",
  "private": true,
  "packageManager": "pnpm@10.27.0",
  "scripts": {
    "check:types": "pnpm -r --parallel check:types",
    "check:lint": "pnpm -r --parallel check:lint",
    "check:format": "pnpm prettier --check .",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "catalog:",
    "prettier": "catalog:",
    "prettier-plugin-tailwindcss": "catalog:"
  }
}
